{
  "name": "mio-viewer-workspace",
  "private": true,
  "workspaces": [
    "packages/mio-parser",
    "packages/mio-viewer"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/mio-viewer",
    "test": "npm run test --workspaces",
    "lint": "eslint packages/*/src --ext .ts,.tsx"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
